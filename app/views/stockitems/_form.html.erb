<%= form_with(model: stockitem) do |form| %>
  <% if stockitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stockitem.errors.count, "error") %> prohibited this stockitem from being saved:</h2>

      <ul>
        <% stockitem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :備蓄品名称 %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :purchase_date %>
    <%= form.date_select :purchase_date %>
  </div>

  <div class="field">
    <%= form.label :expiration_date %>
    <%= form.date_select :expiration_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
